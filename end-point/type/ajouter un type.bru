meta {
  name: ajouter un type
  type: http
  seq: 3
}

post {
  url: {{base_uri}}/type
  body: json
  auth: inherit
}

body:json {
  {
    "name":"test"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
